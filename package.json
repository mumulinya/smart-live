{
  "name": "smart-live-docs",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "docs:sync-data": "node scripts/sync-project-stats.mjs",
    "docs:dev": "npm run docs:sync-data && vitepress dev docs",
    "docs:build": "npm run docs:sync-data && vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.2.2",
    "vue": "^3.4.27"
  }
}

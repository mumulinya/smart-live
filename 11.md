# 智评生活·后端架构重构与核心交易流程设计文档

这份文档汇总了我们从"模块拆分"到"高并发下单"的所有讨论成果。你可以直接把这个作为你的开发蓝图。

## 一、核心架构重构 (Module Architecture)

为了符合微服务"动静分离"和"资产 vs 规则"的设计原则，我们需要对现有模块进行职责重新划分。

### 1. 新增/重构：商品模块 (Product Service)
- **地位：** 核心资产模块（原先你打算放在 Marketing，现已纠正）
- **职责：** 管理所有**"用户花钱买的东西"**
- **包含内容：**
  - 代金券：也是一种商品
  - 团购套餐：包含具体的菜品明细
  - 秒杀商品：本质上还是商品，只是售卖方式不同

### 2. 定位调整：市场模块 (Marketing Service)
- **地位：** 规则辅助模块
- **职责：** 管理所有**"用户免费领/抵扣的规则"**
- **包含内容：** 满减活动、平台红包、优惠券、积分抵扣规则

### 3. 关联模块
- **订单模块 (Order Service)：** 交易核心，负责串联商品和市场
- **钱包模块 (Wallet Service)：** 资金核心，负责混合支付（余额+微信）和收银台逻辑
- **积分模块 (Integral Service)：** 账务核心，只负责加减积分

## 二、数据库核心设计 (Schema Design)

我们采用**"一张大表走天下"**的设计，配合 JSON 字段实现差异化存储。

**表名：shop_goods (商品表)**
**位于 商品模块 数据库中**

```sql
CREATE TABLE `shop_goods` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT,
  `shop_id` bigint(20) NOT NULL,
  `name` varchar(128) NOT NULL,

  -- 核心字段 1：区分"它是什么？"
  `category` tinyint(4) NOT NULL COMMENT '1:代金券, 2:团购套餐',

  -- 核心字段 2：区分"怎么卖？"
  `activity_type` tinyint(4) NOT NULL DEFAULT '0' COMMENT '0:普通售卖, 1:秒杀活动',

  -- 核心字段 3：差异化数据存储
  -- 代金券存：{"value": 100, "limit": 200}
  -- 套餐存：{"dishes": [{"name":"烤鱼","count":1}], "people": 2}
  `rules_json` json DEFAULT NULL,

  `price` decimal(10,2) NOT NULL COMMENT '实际售价(秒杀价/团购价)',
  `original_price` decimal(10,2) NOT NULL COMMENT '原价',
  `stock` int(11) NOT NULL COMMENT '库存',

  PRIMARY KEY (`id`)
);
```

## 三、核心代码模式：策略模式 (Strategy Pattern)

在 商品模块 的 Service 层，使用策略模式来处理"秒杀"和"普通购买"的不同校验逻辑。

### 1. 目录结构
```
com.smartlife.product.service
├── ProductService.java       // 上下文(Context)，负责分发请求
└── strategy
    ├── PurchaseStrategy.java // 接口 (Interface)
    ├── SeckillStrategy.java  // 秒杀策略 (Impl)
    └── NormalStrategy.java   // 普通策略 (Impl)
```

### 2. 逻辑分流 (ProductService)
```java
public Result<Long> createOrder(Long userId, Long goodsId) {
    // 1. 查库获取商品类型
    Goods goods = goodsMapper.selectById(goodsId);

    // 2. 自动匹配策略 (利用 Spring 注入 Map)
    // 假设 bean 名字为 "strategy_0" (普通) 或 "strategy_1" (秒杀)
    String key = "strategy_" + goods.getActivityType();
    PurchaseStrategy strategy = strategyMap.get(key);

    // 3. 执行校验逻辑 (秒杀查时间/限购，普通只查库存)
    strategy.validate(userId, goods);

    // 4. (公共逻辑) 执行通用下单流程...
}
```

## 四、核心交易流程：全异步下单 (Full Async Order)

为了抗住高并发（秒杀）并统一代码逻辑，所有下单（包括普通单）都走 Redis + MQ 模式。

### 阶段 1：商品模块 (生产者)
- 校验 & 扣库存： 调用策略模式校验，执行 Redis decr 扣库存
- 生成 ID： 使用雪花算法提前生成 orderId
- Redis 占位 (关键)：
  - 写入 SET order:status:10086 "CREATING" EX 60s
  - (作用：告诉查询接口，MQ 还在路上，别报错)
- 发送 MQ： 发送订单消息到 RabbitMQ
- 秒回前端： 返回 orderId

### 阶段 2：订单模块 (消费者)
- 监听 MQ： 收到消息
- 落库： 插入 MySQL 订单表
- 清除占位： 删除 Redis 里的 order:status:10086

### 阶段 3：前端查询 (轮询机制)
**前端逻辑：**
拿到 ID 后，不跳转支付，而是弹窗 Loading，每 500ms 调一次查询接口。

**后端查询接口逻辑 (Redis 优先策略)：**
- 先查 Redis：
  - 有 key (CREATING)？ -> 返回 PENDING (前端继续转圈)
- 后查 MySQL：
  - Redis 没 key 了 -> 查数据库
  - 有数据？ -> 返回 SUCCESS (前端跳转支付)
  - 没数据？ -> 返回 FAILED (下单失败)

## 五、支付与钱包 (Wallet Integration)

下单成功后，前端跳转收银台。
- **入口：** 钱包模块 (Wallet Service)
- **混合支付：** 支持"余额 + 微信"组合
- **流程：**
  - 先扣余额（数据库事务）
  - 生成支付流水
  - 调起微信支付
  - 回调： 微信回调 -> 改流水状态 -> 通知订单模块 -> 通知积分模块（加分）

## 总结 (Next Steps)

按照这个文档，你现在的开发顺序应该是：

- **建模块：** 把 product-service 建好，把 shop_goods 表建好
- **写策略：** 在商品模块里把 SeckillStrategy 和 NormalStrategy 写出来
- **通流程：**
  - 写 MQ 发送逻辑（商品模块）
  - 写 MQ 监听逻辑（订单模块）
  - 写"Redis 优先"的查询接口
- **联调：** 前端写一个轮询逻辑，把整个链路跑通

**这个架构既能满足你现在的毕设/项目需求，也具备了互联网大厂的高并发抗压能力！**

---

**创建时间：** 2026-02-18 16:55
**创建者：** 桃桃
**文件位置：** E:\桌面\智评生活后端架构重构与核心交易流程设计文档.md

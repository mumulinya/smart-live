spring.datasource.platform=mysql
db.num=1
db.url.0=jdbc:mysql://smartLive-mysql:3306/smart-live_config?characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true&useUnicode=true&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
db.user=root
db.password=123167

# 控制台路径固定为 /nacos，和本地开发环境保持一致
server.servlet.contextPath=/nacos

nacos.naming.empty-service.auto-clean=true
nacos.naming.empty-service.clean.initial-delay-ms=50000
nacos.naming.empty-service.clean.period-time-ms=30000

management.endpoints.web.exposure.include=*

management.metrics.export.elastic.enabled=false
management.metrics.export.influx.enabled=false

server.tomcat.accesslog.enabled=true
server.tomcat.accesslog.pattern=%h %l %u %t "%r" %s %b %D %{User-Agent}i %{Request-Source}i

server.tomcat.basedir=/home/smartLive/nacos/tomcat/logs

nacos.security.ignore.urls=/,/error,/**/*.css,/**/*.js,/**/*.html,/**/*.map,/**/*.svg,/**/*.png,/**/*.ico,/console-ui/public/**,/v1/auth/**,/v1/console/health/**,/actuator/**,/v1/console/server/**

nacos.core.auth.system.type=nacos
# 关闭鉴权，保持和当前 Docker 启动参数一致
nacos.core.auth.enabled=false

# ??????? (??? Base64 ????????? > 32 ??)
# ????????????????
nacos.core.auth.plugin.nacos.token.secret.key=VGhpc0lzQVNlY3JldEtleVGhhdElzTG9uZ0Vub3VnaEZvclRlc3Rpbmc=

# ???? key/value (?????? identity ????)
nacos.core.auth.server.identity.key=serverIdentity
nacos.core.auth.server.identity.value=security

nacos.istio.mcp.server.enabled=false

